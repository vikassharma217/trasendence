curl -X GET 'http://localhost:9200/user-logs/_search?pretty'

use this to test elasticsearch



c3r6p1% docker exec -it elasticsearch bin/elasticsearch-service-tokens create kibana kibana

mkdir cert
vim  openssl.cnf
cd last_ft_tran/certs/

openssl req -x509 -nodes -newkey rsa:2048 -keyout server.key -out server.cert -days 365 -config ./openssl.cnf

import the key and cert into backend and frontend


mwu@root:~/Desktop/trans1/last_ft_tran$ docker exec -it elasticsearch /usr/share/elasticsearch/bin/elasticsearch-service-tokens create elastic/kibana kibana-token
SERVICE_TOKEN elastic/kibana/kibana-token = AAEAAWVsYXN0aWMva2liYW5hL2tpYmFuYS10b2tlbjp6WjducVRTN1JKbU1HYjNDelFJR3dn
to change kibana password