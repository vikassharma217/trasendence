import { error } from "console";

// export interface LogData {
//     event: string;
//     message: string;
//     method: string;
//     url: string;
//     statusCode: number;
//     username: string | null; // Adjust type if necessary
//     ip: string;
//     userAgent: string;
//     isConnectionSecure: boolean;
//     error?: string; // Optional field for error messages
//     stack?: string; // Optional field for error stack trace);
//     useremail?: string; // Optional field for user email
    
// }



